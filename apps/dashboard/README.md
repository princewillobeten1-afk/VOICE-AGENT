# VoiceSense Dashboard

Customer-facing web application for building, testing, deploying, and observing AI employees.

## Local Development

From the repository root:

```bash
npm.cmd install
npm.cmd run dev -w @voicesense/dashboard
```

The dashboard runs at `http://localhost:3000`.
