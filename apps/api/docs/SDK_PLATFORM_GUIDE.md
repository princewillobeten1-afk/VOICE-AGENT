# SDK Platform Guide

Official SDK targets are TypeScript/JavaScript, Python, Go, Java, C#, and PHP.

Each SDK should include authentication helpers, typed models, retry logic, error classes, pagination helpers, streaming helpers, webhook verification, and generated examples from the OpenAPI specification.