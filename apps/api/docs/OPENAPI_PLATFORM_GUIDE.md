# OpenAPI Platform Guide

VoiceSense maintains an OpenAPI 3.1 contract for machine-readable schemas, example requests, example responses, SDK generation, API explorer execution, and documentation synchronization.

API versions should reference the OpenAPI artifact that generated compatible SDKs and code samples.