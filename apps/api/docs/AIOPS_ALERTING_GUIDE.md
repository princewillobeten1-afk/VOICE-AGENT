# Alerting Guide

Alerts are configured in `ops_alerts` and runtime incidents are stored in `ops_alert_events`.

Supported alert examples include high error rate, workflow failure, slow response time, cost threshold exceeded, tool failure, provider outage, knowledge index failure, and memory growth limits.

Notification delivery is modeled through channels but actual delivery should use the Notification System in a future task.
