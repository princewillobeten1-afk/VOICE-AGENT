# Cost Intelligence Guide

AIOps cost records track LLM, STT, TTS, embedding, vector database, tool, workflow, and infrastructure costs.

Costs can be linked to AI employees, conversations, workflows, providers, or arbitrary platform entities through `entity_type` and `entity_id`.

Forecasting and budget optimization are intentionally deferred until enough real cost telemetry exists.
