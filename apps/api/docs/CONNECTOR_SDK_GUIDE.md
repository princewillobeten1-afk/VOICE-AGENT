# Connector SDK Guide

The connector SDK contract standardizes install, authenticate, validate permissions, sync, execute actions, receive triggers, publish versions, test connections, log events, and surface errors.

SDK helpers should include OAuth, PKCE, API key, JWT, service-account, basic-auth, custom-auth, webhook verification, retry, logging, and fixture-based testing utilities.