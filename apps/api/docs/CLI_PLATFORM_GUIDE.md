# CLI Platform Guide

The VoiceSense CLI command surface includes login, logout, init, deploy, validate, test, logs, workflows, employees, knowledge, integrations, and api-keys.

The CLI should support interactive workflows for humans and scriptable output for automation.